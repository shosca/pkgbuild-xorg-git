# Maintainer: Joel Teichroeb <joel@teichroeb.net>
# Contributor: Emmanuel Gil Peyrot <linkmauve@linkmauve.fr>

_realver=1.4.0
_gitroot="git://anongit.freedesktop.org/wayland/weston"
_gitname="weston"

pkgname=weston-git
pkgver=20140227
pkgrel=1218
pkgdesc="Reference implementation of a Wayland compositor"
arch=(i686 x86_64)
url="http://wayland.freedesktop.org"
license=('MIT')
depends=('libxkbcommon-git' 'wayland-git' 'mesa-git' 'cairo-git' 'libunwind'
         'poppler-glib' 'mtdev' 'libxcursor-git' 'pixman-git' 'glu-git')
makedepends=('git')
options=(!libtool)
provides=('weston=${_realver}')
source=($_gitroot)
md5sums=(SKIP)

build() {
  cd "${srcdir}/${_gitname}"

  ./autogen.sh \
    --prefix=/usr \
    --libexecdir=/usr/lib/weston \
    --disable-android-compositor

  make
}

package() {
  cd "${srcdir}/${_gitname}"
  make DESTDIR="${pkgdir}" install
  install -Dm644 COPYING "${pkgdir}/usr/share/licenses/${pkgname}/COPYING"
}

# vim: ft=sh syn=sh et
