# Maintainer: Det <who@cares.com>
# Contributor: <Eugeny Borisov flekst@gmail.com>
# Based on [extra]'s xf86-input-keyboard

_gitroot=git://anongit.freedesktop.org/xorg/driver/xf86-input-keyboard
_gitname=xf86-input-keyboard

pkgname=xf86-input-keyboard-git
pkgver=r242.ccf63a6
pkgrel=22
pkgdesc="X.Org keyboard Input driver - Git version"
arch=('i686' 'x86_64')
url="http://cgit.freedesktop.org/xorg/driver/xf86-input-keyboard/"
license=('custom')
depends=('glibc')
makedepends=('xorg-server-devel-git' 'resourceproto-git' 'scrnsaverproto-git' 'git')
groups=('xorg-drivers' 'xorg')
provides=("xf86-input-keyboard")
conflicts=('xf86-input-keyboard')
replaces=('xf86-input-keyboard')
source=($_gitroot)
sha256sums=('SKIP')

pkgver() {
  cd "$srcdir/$_gitname"
  printf "r%s.%s" "$(git rev-list --count HEAD)" "$(git rev-parse --short HEAD)"
}

build() {
  cd "${srcdir}/${_gitname}"

  ./autogen.sh --prefix=/usr
  make
}

package(){
  cd "${srcdir}/${_gitname}"
  make DESTDIR="${pkgdir}" install
}
# vim: ft=sh syn=sh et

