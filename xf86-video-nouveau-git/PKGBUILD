# $Id: PKGBUILD 174803 2013-01-07 18:10:24Z andyrtr $
# Maintainer: Andreas Radke <andyrtr@archlinux.org>
# Contributor: buddabrod <buddabrod@gmail.com>

_gitroot="git://anongit.freedesktop.org/nouveau/xf86-video-nouveau"
_gitname="xf86-video-nouveau"

pkgname=xf86-video-nouveau-git
pkgver=r2413.762b22f
pkgrel=6
pkgdesc="Open Source 2D acceleration driver for nVidia cards"
arch=('i686' 'x86_64')
url="http://nouveau.freedesktop.org/"
license=('GPL')
depends=('libdrm-git' 'udev' 'mesa-dri-git')
makedepends=('xorg-server-devel-git' 'git')
provides=('xf86-video-nouveau')
conflicts=('xf86-video-nouveau' 'xorg-server<1.15.99')
replaces=('xf86-video-nouveau')
groups=('xorg-drivers' 'xorg')
install=$pkgname.install
source=($_gitroot)
sha256sums=('SKIP')

pkgver() {
  cd "$srcdir/$_gitname"
  printf "r%s.%s" "$(git rev-list --count HEAD)" "$(git rev-parse --short HEAD)"
}

build() {
  cd "${srcdir}/${_gitname}"

  ./autogen.sh --prefix=/usr --disable-glamor
  make
}

package() {
  cd "${srcdir}/${_gitname}"
  make DESTDIR="$pkgdir" install
}

# vim: ft=sh syn=sh et

