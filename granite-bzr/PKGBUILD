# Maintainer: Ner0

pkgname=granite-bzr
pkgver=475
pkgrel=2
pkgdesc="A development library for elementary development"
arch=('i686' 'x86_64')
url="https://launchpad.net/granite"
license=('LGPL3')
groups=('pantheon')
depends=('glib2' 'gobject-introspection' 'gtk3' 'libgee' 'hicolor-icon-theme')
makedepends=('bzr' 'cmake' 'vala')
conflicts=('granite')
provides=('granite')
install=$pkgname.install
source=('granite-0.1.1.tar.gz')
md5sums=('1bc0bc2df9176940097a26f3d031034a')

_bzrmod=granite-0.1.1

build() {
  cp -r "$srcdir/$_bzrmod" "$srcdir/$_bzrmod-build"
  cd "$srcdir/granite-0.1.1-build"
  sed -i '/--warn-all/d' cmake/GObjectIntrospectionMacros.cmake
  rm -rf build/
  mkdir build
  cd build
  CFLAGS="${CFLAGS} -Wno-deprecated-declarations"
  cmake .. -DCMAKE_INSTALL_PREFIX=/usr -DICON_UPDATE=OFF
  make
}

package() {
  cd "$_bzrmod-build/build"
  make DESTDIR="$pkgdir/" install
}
