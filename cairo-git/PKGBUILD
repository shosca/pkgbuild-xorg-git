# Maintainer: dx <dx@dxzone.com.ar>
# Based on the official cairo package

_realver=1.12.15
_gitroot='git://anongit.freedesktop.org/git/cairo'
_gitname='cairo'

pkgname=cairo-git
pkgver=20130716
pkgrel=0713
pkgdesc="Cairo vector graphics library (git version)"
arch=(i686 x86_64)
license=('LGPL' 'MPL')
url="http://cairographics.org/"
depends=('libpng>=1.4.0' 'libxrender>=0.9.7' 'fontconfig>=2.8.0' 'pixman' 'xcb-util>=0.3.6' 'glib2>=2.24.0')
makedepends=('gtk-doc' 'git')
options=('!libtool')
provides=('cairo=${_realver}' 'cairo-xcb=${_realver}')
replaces=('cairo')
conflicts=('cairo' 'cairo-xcb')
source=($_gitroot)
md5sums=(SKIP)

build() {
  cd "${srcdir}/${_gitname}"

  ./autogen.sh \
    --prefix=/usr \
    --sysconfdir=/etc \
    --localstatedir=/var \
    --enable-xcb \
    --enable-tee \
    --enable-gl \
    --enable-egl \
    --disable-static
  make
}

package(){
  cd "${srcdir}/${_gitname}"
  make DESTDIR="${pkgdir}" install
}

# vim: ft=sh syn=sh et

